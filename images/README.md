# QuizHub image assets

Put question and answer images in this directory. Reference them from JSON with paths such as `images/network-diagram.png`.

Supported formats depend on the browser, including PNG, JPEG, GIF, SVG, and WebP.
